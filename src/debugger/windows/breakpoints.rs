// The breakpoint engine is implemented inline in `backend.rs` so it can
// share state with the debug-event loop (saved bytes, pending re-insert,
// trap flag handling, etc.). This file is intentionally a placeholder
// to match the requested project layout.
